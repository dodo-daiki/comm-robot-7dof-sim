# CLAUDE.md

このファイルは、このリポジトリで作業する AI エージェント(Claude Sonnet / Claude Fable / GitHub Copilot CLI)向けのガイドです。

## プロジェクト概要

7自由度(7-DOF)コミュニケーションロボットの骨格・外装設計を、ハードウェア確定前にブラウザ上でおおまかに検討するためのシミュレーター。

- `viewer/robot_viewer.html` — **メインツール**。単一の自己完結HTMLファイル(Three.js完全インライン、外部依存・ネットワーク通信ゼロ)。開発の主戦場はここ。
- `robot/`, `data/`, `tests/` — **Pythonリファレンス実装**。運動学・PETG質量推定・GIM4310モータースペック・静的重力トルク計算。`viewer/robot_viewer.html` 内のJS実装はここから移植され、各マイルストーンでクロス検証されている「正解データ」。
- `README.md` — エンドユーザー向け説明。

詳細な設計判断・既知の制約(腰ピッチなし、肩ロール/ヨーなし等)は `README.md` を参照。

## マルチエージェント体制

このプロジェクトは複数のAIエージェントを役割分担して開発している。新しくこの体制に参加するエージェントは以下を踏まえること。

| 役割 | 担当 | 呼び出し方 |
|---|---|---|
| 統括・要件整理・タスク分解 | このセッション自身(Claude Code, 実行時のモデルは固定) | — |
| 実装 | Claude Sonnet サブエージェント。トークン逼迫時は GitHub Copilot CLI にフォールバック | `Agent` tool, `model: "sonnet"` |
| コードレビュー・検証 | GitHub Copilot CLI(非対話モード) | `gh copilot -p "<prompt>" --allow-tool '...'` (Bash経由) |
| 最終確認(公開前サインオフ) | Claude Fable サブエージェント | `Agent` tool, `model: "fable"` |

**注意**: GitHub Copilot CLI は Claude のトークン予算とは別に「AI Credits」を消費する。Claude側のトークンとCopilot側のクレジット、双方を無駄遣いしないことを常に意識する。

### トークン/クレジット節約の方針

これまでの開発(M1〜M6)では、1マイルストーンごとに独立したエージェントが数十万トークン規模でブラウザ自動化・数百パターンの数値検証・ミューテーションテストまで行う、非常に重厚な実装→検証ループを回してきた。これは物理演算(運動学・トルク計算)の正しさを担保する初期フェーズでは有効だったが、今後は以下の方針でコストを抑える。

- **検証は差分(diff)にスコープを絞る**。変更されていないコードパス(既にPythonとクロス検証済みのFK数式本体など)を毎回フルに再検証しない。前回までの検証結果を信頼し、実際に変更された箇所とその影響範囲だけを重点的に見る。
- **ブラウザ自動化による動的検証は、物理・数値計算に関わる変更のときだけ**行う。UIレイアウトや文言だけの変更は、静的なコードレビュー(読み合わせ)で十分なことが多い。
- **ミューテーションテストや数百パターンの総当たり検証は、過去に一度そのロジックで実施済みなら省略してよい**。同じロジックを使い回している限り、再度ゼロからやり直す必要はない。
- レビュー依頼のプロンプトは、変更点・懸念点を絞って具体的に伝える。ファイル全体を毎回読み直させるような曖昧な依頼を避ける。

### 実装エージェントへの指示の書き方

- 変更対象ファイルは基本的に `viewer/robot_viewer.html` のみ(Pythonのリファレンス実装を変更する場合は明示する)。
- 既存の検証済みロジック(FKの合成順序、`R_mount` の挿入位置、`worldAxis` の計算など)は**変更しない**ことを明示する。
- 新しいコミットを作る(amendしない)。pushはしない(ユーザーの指示があるまで)。
- 実キーボードイベントでの入力検証を求める(`.value=` の直接代入による検証はこれまで複数回バグを見逃した実績があるため)。

## コミット規約

- 1マイルストーン・1修正 = 1コミット。`git commit --amend` は使わない。
- コミットメッセージは変更内容を簡潔に(このリポジトリの既存の `git log` を参考にする)。
- `git push` はユーザーから明示的に指示されたときのみ。

## 開発履歴の要約

`git log --oneline` を参照。主要マイルストーン:

- M1: 関節ツリーと順運動学(Python)
- M2: PETG質量推定・モータースペック・静的トルク計算(Python)
- M3: Three.jsマネキンビューア(JS、Pythonと逐次クロス検証)+ ちびキャラ風デザインへの作り直し
- M4: サンプル動作再生 + カスタム動作エディタ(複数軸キーフレーム)
- M5: トルク・負荷パネル(JS側にPythonの物理演算を移植)
- M6: README/LICENSE整備 + GitHub公開
- M7: 骨格パーツ単位のUI再編成(「骨格パーツ」/「関節」パネル分離)+ 3Dモデルのクリック選択 + パーツ重量の直接指定

## 既知の制約

- 腰ピッチ軸がない(お辞儀のような前傾ができない)
- 肩のロール/ヨー軸がない(腕を横に振れない)

これらは実機の関節レイアウトを見直す際の参考情報であり、シミュレーター側のバグではない。
